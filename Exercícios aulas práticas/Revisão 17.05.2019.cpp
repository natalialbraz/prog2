#include<iostream>
using namespace std;

//______________________________________ FUNÇÃO FATORIAL RECURSIVA _______________________________________________

/*
double fatorial(int n){
    if (n==0){
        return 1;
    }
    return n*fatorial(n-1);
}

int main(){
    cout<<fatorial(4)<<endl;
    return 0;
}
*/

//___________________________________ FUNÇÃO PARA TROCA DE DUAS VARIÁVEIS _______________________________________

/*
void troca(int &x, int &y){
    int aux=x;
    x=y;
    y=aux;
}
int main(){
    int a=2, b=7;
    cout<<"a = "<<a<<"\nb = "<<b;
    troca(a, b);
    cout<<"\n\nApos troca:\na = "<<a<<"\nb = "<<b<<endl;
    return 0;
}
*/

//_______________________________________________________________________________________________________________

/*

*/


/*
_________________________________________________________________________________________________________________
-----------------------------------------------------------------------------------------------------------------

-> tipos primitivos:
int, float, double, char, void, BOOL(assume apenas 0 ou 1 - false ou true)

-> tipos abstratos:
struct; classes(string, vector, array)

-> modificadores:
*(ponteiro); &(endereço de meória);
long(aumenta tamanho de memória do número - pode ser usado no máximo 2 vezes);
short(diminui tamanho de memória do número - pode ser usado no máximo 1 vez);
signed(vem por padrão, aceita numeros positivos e negativos - pode ser usado no máximo 1 vez);
unsigned(aceita apenas números positivos, útil em índices de vetores - pode ser usado no máximo 1 vez)

funções do tipo int, float, double requerem um retorno, void não aceita;



*/
